function verificarPar(numero) {
    if (numero % 2 === 0) {
      console.log(`${numero} é par.`);
    } else {
      console.log(`${numero} não é par.`);
    }
  }
  

  const numeroParaVerificar = 3;
  verificarParOuNaoPar(numeroParaVerificar);
  